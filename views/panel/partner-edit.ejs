<%- include('header') %>
<%
    const userRole = (user && user.type || "").toLowerCase();
    const isStaff = ['host', 'staff', 'owner'].some(r => userRole.includes(r));
%>

<div class="staff-hub">

    <!-- Top Banner Header -->
    <div class="staff-section" style="display: flex; align-items: center; justify-content: space-between; flex-wrap: wrap; gap: 15px; margin-bottom: 20px;">
        <div>
            <h2 style="margin: 0; font-size: 1.5rem;">Partner Club Profile</h2>
            <p style="font-size: 0.8rem; color: #888; margin: 5px 0 0 0;">
                Customize your club's showcase details, custom URL slug, accent color, banner, icon, and community links.
            </p>
        </div>
        <% if (partner && partner.slug) { %>
            <a href="/partner/<%= partner.slug %>" target="_blank" class="btn-cc btn-secondary btn-small" style="text-decoration: none;">
                🌐 View Live Page ↗
            </a>
        <% } %>
    </div>

    <!-- MAIN GRID: Form & Live Preview -->
    <div style="display: grid; grid-template-columns: 1fr 360px; gap: 20px; align-items: start;">
        
        <!-- LEFT: Form Controls & Event Manager -->
        <div style="display: flex; flex-direction: column; gap: 20px;">
            
            <div class="staff-section">
                <h3 style="margin-top: 0; margin-bottom: 20px; font-size: 1.1rem; color: #fff; border-bottom: 1px solid rgba(255,255,255,0.08); padding-bottom: 10px;">
                    ⚙️ Club Info & Branding
                </h3>

                <form id="partner-form" onsubmit="handlePartnerSubmit(event)">
                    <% if (partner && partner.id) { %>
                        <input type="hidden" id="partner-id" value="<%= partner.id %>">
                    <% } %>

                    <div class="form-group">
                        <label>Club / Community Name *</label>
                        <input type="text" id="partner-name" value="<%= partner ? partner.name : '' %>" placeholder="e.g. Neon Critters Club" required>
                    </div>

                    <!-- CUSTOM URL SLUG -->
                    <div class="form-group">
                        <label>Custom Showcase URL Slug *</label>
                        <div style="display: flex; align-items: center; gap: 8px;">
                            <span style="color: #888; font-size: 0.85rem; font-weight: bold; background: rgba(0,0,0,0.3); padding: 10px 12px; border-radius: 8px; border: 1px solid rgba(255,255,255,0.1);">/partner/</span>
                            <input type="text" id="partner-slug" value="<%= partner ? partner.slug : '' %>" placeholder="my-club-slug" required style="flex: 1;">
                        </div>
                        <p style="font-size: 0.75rem; color: #777; margin-top: 4px;">Only lowercase letters, numbers, and hyphens.</p>
                    </div>

                    <!-- BRAND ACCENT COLOR -->
                    <div class="form-group">
                        <label>Brand Accent Color</label>
                        <div style="display: flex; align-items: center; gap: 10px;">
                            <input type="color" id="partner-color" value="<%= (partner && partner.accentColor) ? partner.accentColor : '#f2008d' %>" style="width: 45px; height: 40px; border: none; border-radius: 8px; cursor: pointer; background: transparent;" oninput="document.getElementById('partner-color-hex').value=this.value; updatePreviewColor(this.value)">
                            <input type="text" id="partner-color-hex" value="<%= (partner && partner.accentColor) ? partner.accentColor : '#f2008d' %>" placeholder="#f2008d" style="flex: 1;" oninput="document.getElementById('partner-color').value=this.value; updatePreviewColor(this.value)">
                        </div>
                    </div>

                    <div class="form-group">
                        <label>Club Description / Bio</label>
                        <textarea id="partner-description" rows="5" placeholder="Tell the community about your club, genres, event times, and vibe..."><%= partner ? partner.description : '' %></textarea>
                    </div>

                    <div style="display: grid; grid-template-columns: 1fr 1fr; gap: 15px; margin-bottom: 20px;">
                        <!-- ICON UPLOAD -->
                        <div style="background: rgba(0,0,0,0.2); border: 1px dashed rgba(255,255,255,0.15); border-radius: 10px; padding: 15px; text-align: center;">
                            <label style="font-size: 0.8rem; font-weight: bold; color: var(--primary-blue); display: block; margin-bottom: 10px;">
                                🖼️ Club Logo / Icon
                            </label>
                            <div id="icon-preview-box" style="width: 75px; height: 75px; border-radius: 50%; margin: 0 auto 10px auto; overflow: hidden; background: #111; border: 2px solid <%= (partner && partner.accentColor) ? partner.accentColor : '#f2008d' %>; display: flex; align-items: center; justify-content: center;">
                                <img id="icon-preview" src="<%= (partner && partner.iconUrl) ? partner.iconUrl : '/cdn/logos/club/Logo.png' %>" style="width: 100%; height: 100%; object-fit: cover;">
                            </div>
                            <input type="file" id="icon-file-input" accept="image/*" style="display: none;" onchange="uploadMedia('icon')">
                            <button type="button" class="btn-cc btn-small btn-secondary" onclick="document.getElementById('icon-file-input').click()" style="width: 100%;">
                                Upload Icon
                            </button>
                        </div>

                        <!-- BANNER UPLOAD -->
                        <div style="background: rgba(0,0,0,0.2); border: 1px dashed rgba(255,255,255,0.15); border-radius: 10px; padding: 15px; text-align: center;">
                            <label style="font-size: 0.8rem; font-weight: bold; color: var(--primary-pink); display: block; margin-bottom: 10px;">
                                🎨 Header Banner
                            </label>
                            <div id="banner-preview-box" style="width: 100%; height: 75px; border-radius: 6px; margin: 0 auto 10px auto; overflow: hidden; background: #111; border: 1px solid rgba(255,255,255,0.2); display: flex; align-items: center; justify-content: center;">
                                <img id="banner-preview" src="<%= (partner && partner.bannerUrl) ? partner.bannerUrl : '/cdn/logos/club/banner_fallback.jpg' %>" style="width: 100%; height: 100%; object-fit: cover;" onerror="this.src='/cdn/logos/club/Logo.png'">
                            </div>
                            <input type="file" id="banner-file-input" accept="image/*" style="display: none;" onchange="uploadMedia('banner')">
                            <button type="button" class="btn-cc btn-small btn-secondary" onclick="document.getElementById('banner-file-input').click()" style="width: 100%;">
                                Upload Banner
                            </button>
                        </div>
                    </div>

                    <h4 style="margin-top: 20px; margin-bottom: 12px; color: #fff; font-size: 0.95rem; border-bottom: 1px solid rgba(255,255,255,0.05); padding-bottom: 6px;">
                        🔗 Community Links
                    </h4>

                    <div class="form-group">
                        <label>VRChat Group Link / Shortname</label>
                        <input type="url" id="partner-vrcgroup" value="<%= partner ? partner.vrcGroupUrl : '' %>" placeholder="https://vrc.group/FURN.9601">
                    </div>

                    <div class="form-group">
                        <label>Discord Invite Link</label>
                        <input type="url" id="partner-discord" value="<%= partner ? partner.discordInvite : '' %>" placeholder="https://discord.gg/yourclub">
                    </div>

                    <div class="form-group" style="margin-bottom: 25px;">
                        <label>Official Website URL (Optional)</label>
                        <input type="url" id="partner-website" value="<%= partner ? partner.websiteUrl : '' %>" placeholder="https://yourclub.com">
                    </div>

                    <button type="submit" class="btn-cc btn-primary" style="width: 100%; padding: 12px; font-weight: bold;">
                        💾 Save Partner Profile
                    </button>
                </form>
            </div>

            <% if (partner && partner.id) { %>
                <!-- PARTNER EVENTS SCHEDULE MANAGER -->
                <div class="staff-section">
                    <div style="display: flex; align-items: center; justify-content: space-between; margin-bottom: 15px;">
                        <h3 style="margin: 0; font-size: 1.1rem; color: #fff;">
                            📅 Upcoming Partner Events
                        </h3>
                        <button type="button" class="btn-cc btn-small btn-primary" onclick="openAddEventModal()">
                            ➕ Add Event
                        </button>
                    </div>

                    <div id="partner-events-list">
                        <% if (partner.events && partner.events.length > 0) { %>
                            <% partner.events.forEach(evt => { %>
                                <div style="background: rgba(0,0,0,0.25); border: 1px solid rgba(255,255,255,0.08); border-radius: 12px; overflow: hidden; margin-bottom: 12px; display: flex; flex-direction: column;">
                                    <% if (evt.bannerUrl) { %>
                                        <div style="height: 100px; position: relative; background: #0c1219;">
                                            <img src="<%= evt.bannerUrl %>" alt="<%= evt.title %> Flyer" style="width: 100%; height: 100%; object-fit: cover;">
                                        </div>
                                    <% } %>
                                    <div style="padding: 15px;">
                                        <%
                                            const now = new Date();
                                            const start = new Date(evt.startTime);
                                            const end = new Date(evt.endTime);
                                            const isLive = now >= start && now < end;
                                            const isEnded = now >= end;
                                            let badgeText = isLive ? 'LIVE NOW' : (isEnded ? 'ENDED' : 'UPCOMING');
                                            let badgeStyle = isLive ? 'background: rgba(255,23,68,0.2); color: #ff1744; border: 1px solid #ff1744;' : (isEnded ? 'background: rgba(255,255,255,0.05); color: #888; border: 1px solid #444;' : 'background: rgba(41,197,246,0.15); color: #29c5f6; border: 1px solid #29c5f6;');
                                        %>
                                        
                                        <div style="display: flex; align-items: center; justify-content: space-between; gap: 10px; flex-wrap: wrap; margin-bottom: 8px;">
                                            <div style="display: flex; align-items: center; gap: 8px; flex-wrap: wrap;">
                                                <h4 style="margin: 0; color: #fff; font-size: 1rem;"><%= evt.title %></h4>
                                                <span style="font-size: 0.65rem; font-weight: bold; padding: 2px 8px; border-radius: 12px; <%= badgeStyle %>"><%= badgeText %></span>
                                            </div>
                                            <span style="font-size: 0.78rem; color: <%= partner.accentColor || 'var(--primary-pink)' %>; font-weight: bold;">
                                                🕒 <%= new Date(evt.startTime).toLocaleString([], { dateStyle: 'medium', timeStyle: 'short' }) %> &rarr; <%= new Date(evt.endTime).toLocaleTimeString([], { timeStyle: 'short' }) %>
                                            </span>
                                        </div>

                                        <% if (evt.description) { %>
                                            <div style="font-size: 0.8rem; color: #aaa; line-height: 1.4; white-space: pre-wrap; margin-bottom: 10px;"><%= evt.description %></div>
                                        <% } %>

                                        <div style="display: flex; align-items: center; justify-content: flex-end; gap: 8px; border-top: 1px solid rgba(255,255,255,0.06); padding-top: 10px; margin-top: 10px; flex-wrap: wrap;">
                                            <% if (evt.hasPostedDiscordLive || evt.hasPostedDiscordUpcoming) { %>
                                                <button type="button" class="btn-cc btn-small" disabled style="background: rgba(255,255,255,0.04); border: 1px solid #444; color: #777; cursor: not-allowed;" title="<%= evt.hasPostedDiscordLive ? 'Live announcement posted to Discord.' : 'Initial announcement posted. When live, our bot will automatically post a live announcement.' %>">
                                                    <%= evt.hasPostedDiscordLive ? '✓ Live Announcement Posted' : '✓ Announced to Discord' %>
                                                </button>
                                            <% } else { %>
                                                <button type="button" class="btn-cc btn-small btn-secondary" style="background: rgba(88, 101, 242, 0.2); border: 1px solid #5865F2; color: #5865F2;" onclick="openDiscordPostModal(<%= evt.id %>, '<%= evt.title.replace(/'/g, "\\'") %>')">
                                                    📢 Post to Discord
                                                </button>
                                            <% } %>
                                            <button type="button" class="btn-cc btn-small btn-dark" onclick="triggerEditEvent(<%= evt.id %>)">
                                                Edit
                                            </button>
                                            <button type="button" class="btn-cc btn-small btn-danger" onclick="openDeleteEventModal(<%= evt.id %>)">
                                                Delete
                                            </button>
                                        </div>
                                    </div>
                                </div>
                            <% }) %>
                        <% } else { %>
                            <p style="font-size: 0.8rem; color: #777; font-style: italic; margin: 0;">No events added yet. Click 'Add Event' above to list an upcoming event!</p>
                        <% } %>
                    </div>
                </div>
            <% } %>

            <!-- CO-OWNERS & TEAM SECTION -->
            <% if (partner && partner.id) { %>
                <div class="staff-section">
                    <div style="display: flex; align-items: center; justify-content: space-between; flex-wrap: wrap; gap: 10px; margin-bottom: 15px; border-bottom: 1px solid rgba(255,255,255,0.08); padding-bottom: 10px;">
                        <div>
                            <h3 style="margin: 0; font-size: 1.1rem; color: #fff;">
                                👥 Club Leads & Co-Owners
                            </h3>
                            <p style="font-size: 0.78rem; color: #888; margin: 4px 0 0 0;">
                                Co-owners have full permissions to edit club branding and manage showcase events.
                            </p>
                        </div>
                        <% if (!isPrimaryOwner) { %>
                            <span style="font-size: 0.75rem; background: rgba(88, 101, 242, 0.15); border: 1px solid #5865f2; color: #5865f2; padding: 4px 10px; border-radius: 20px; font-weight: bold;">
                                🤝 You are a Co-Owner
                            </span>
                        <% } %>
                    </div>

                    <!-- Primary Owner Card -->
                    <div style="margin-bottom: 15px;">
                        <label style="font-size: 0.75rem; color: #888; text-transform: uppercase; letter-spacing: 0.5px; margin-bottom: 6px; display: block;">Primary Club Owner</label>
                        <div style="background: rgba(255, 215, 0, 0.05); border: 1px solid rgba(255, 215, 0, 0.3); border-radius: 10px; padding: 12px 15px; display: flex; align-items: center; justify-content: space-between; gap: 15px;">
                            <div style="display: flex; align-items: center; gap: 12px;">
                                <img src="<%= (partner.owner && partner.owner.imageUrl) ? partner.owner.imageUrl : '/cdn/logos/club/Logo.png' %>" style="width: 40px; height: 40px; border-radius: 50%; object-fit: cover; border: 2px solid #ffd700;">
                                <div>
                                    <div style="font-weight: bold; color: #fff; font-size: 0.95rem;">
                                        <%= partner.owner ? partner.owner.name : 'Unknown User' %>
                                    </div>
                                    <div style="font-size: 0.75rem; color: #aaa;">ID: <code style="color: #ffd700;"><%= partner.ownerDiscordId %></code></div>
                                </div>
                            </div>
                            <span style="font-size: 0.75rem; background: rgba(255, 215, 0, 0.2); border: 1px solid #ffd700; color: #ffd700; padding: 3px 10px; border-radius: 12px; font-weight: bold;">
                                👑 Primary Owner
                            </span>
                        </div>
                    </div>

                    <!-- Co-Owners List -->
                    <div>
                        <label style="font-size: 0.75rem; color: #888; text-transform: uppercase; letter-spacing: 0.5px; margin-bottom: 6px; display: block;">Active Co-Owners</label>
                        <div id="co-owners-list" style="display: flex; flex-direction: column; gap: 8px;">
                            <% if (typeof coOwners !== 'undefined' && coOwners && coOwners.length > 0) { %>
                                <% coOwners.forEach(co => { %>
                                    <div class="co-owner-item" data-id="<%= co.discordId %>" style="background: rgba(255, 255, 255, 0.03); border: 1px solid rgba(255, 255, 255, 0.08); border-radius: 10px; padding: 10px 15px; display: flex; align-items: center; justify-content: space-between; gap: 12px; transition: all 0.2s ease;">
                                        <div style="display: flex; align-items: center; gap: 12px;">
                                            <img src="<%= co.imageUrl || '/cdn/logos/club/Logo.png' %>" style="width: 36px; height: 36px; border-radius: 50%; object-fit: cover; border: 1px solid #5865f2;">
                                            <div>
                                                <div style="font-weight: bold; color: #fff; font-size: 0.9rem;"><%= co.name %></div>
                                                <div style="font-size: 0.72rem; color: #777;">ID: <code><%= co.discordId %></code></div>
                                            </div>
                                        </div>
                                        <div style="display: flex; align-items: center; gap: 8px;">
                                            <span style="font-size: 0.72rem; background: rgba(88, 101, 242, 0.15); border: 1px solid rgba(88, 101, 242, 0.4); color: #5865f2; padding: 2px 8px; border-radius: 10px;">
                                                🤝 Co-Owner
                                            </span>
                                            <% if (isPrimaryOwner) { %>
                                                <button type="button" class="btn-cc btn-small" style="font-size: 0.72rem; padding: 3px 8px; background: rgba(255, 215, 0, 0.1); border: 1px solid #ffd700; color: #ffd700;" onclick="openTransferModal('<%= co.discordId %>', '<%= co.name.replace(/'/g, "\\'") %>')">
                                                    👑 Transfer Owner
                                                </button>
                                                <button type="button" class="btn-cc btn-small btn-danger" style="font-size: 0.72rem; padding: 3px 8px;" onclick="openRemoveCoOwnerModal('<%= co.discordId %>', '<%= co.name.replace(/'/g, "\\'") %>')">
                                                    ✕ Remove
                                                </button>
                                            <% } %>
                                        </div>
                                    </div>
                                <% }) %>
                            <% } else { %>
                                <p id="no-co-owners-msg" style="font-size: 0.8rem; color: #777; font-style: italic; margin: 0; padding: 10px; background: rgba(0,0,0,0.2); border-radius: 8px;">
                                    No co-owners assigned yet.
                                </p>
                            <% } %>
                        </div>
                    </div>

                    <!-- Add Co-Owner Form (Primary Owner / Staff Only) -->
                    <% if (isPrimaryOwner) { %>
                        <div style="margin-top: 15px; padding-top: 15px; border-top: 1px dashed rgba(255,255,255,0.1);">
                            <label style="font-size: 0.8rem; color: #fff; font-weight: bold; margin-bottom: 6px; display: block;">
                                ➕ Add New Co-Owner
                            </label>
                            <div style="display: flex; gap: 10px; align-items: center;">
                                <input type="text" id="add-co-owner-input" placeholder="Paste Discord ID (e.g. 123456789012345678)" style="flex: 1; font-size: 0.85rem;">
                                <button type="button" class="btn-cc btn-primary btn-small" onclick="addCoOwner()" style="white-space: nowrap;">
                                    + Add Co-Owner
                                </button>
                            </div>
                            <p style="font-size: 0.72rem; color: #777; margin-top: 5px; margin-bottom: 0;">
                                Note: Adding a co-owner grants them immediate edit and event management access to this club profile.
                            </p>
                        </div>
                    <% } %>
                </div>
            <% } %>

        </div>

        <!-- RIGHT: Live Showcase Preview -->
        <div class="staff-section" style="position: sticky; top: 90px; margin-bottom: 0;">
            <h3 style="margin-top: 0; margin-bottom: 15px; font-size: 0.85rem; color: #888; text-transform: uppercase; letter-spacing: 1px;">
                👁️ Live Showcase Preview
            </h3>

            <!-- Partner Card Preview -->
            <div id="preview-card" style="background: rgba(18, 24, 33, 0.95); border: 1px solid <%= (partner && partner.accentColor) ? partner.accentColor : 'rgba(242, 0, 141, 0.3)' %>; border-radius: 14px; overflow: hidden; box-shadow: 0 10px 30px rgba(0,0,0,0.5);">
                <div style="height: 100px; position: relative; background: #0a0e14; overflow: hidden;">
                    <img id="card-banner-preview" src="<%= (partner && partner.bannerUrl) ? partner.bannerUrl : '/cdn/logos/club/banner_fallback.jpg' %>" style="width: 100%; height: 100%; object-fit: cover;" onerror="this.src='/cdn/logos/club/Logo.png'">
                    <div style="position: absolute; inset: 0; background: linear-gradient(to bottom, transparent 40%, rgba(18, 24, 33, 0.9) 100%);"></div>
                </div>

                <div style="padding: 0 15px 15px 15px; position: relative; margin-top: -35px;">
                    <div id="preview-icon-border" style="width: 60px; height: 60px; border-radius: 50%; border: 3px solid #121821; overflow: hidden; background: #111; box-shadow: 0 5px 15px rgba(0,0,0,0.5); margin-bottom: 10px;">
                        <img id="card-icon-preview" src="<%= (partner && partner.iconUrl) ? partner.iconUrl : '/cdn/logos/club/Logo.png' %>" style="width: 100%; height: 100%; object-fit: cover;">
                    </div>

                    <h4 id="card-name-preview" style="margin: 0 0 5px 0; font-size: 1.1rem; color: #fff;">
                        <%= partner ? partner.name : 'Your Club Name' %>
                    </h4>

                    <p id="card-desc-preview" style="margin: 0 0 12px 0; font-size: 0.78rem; color: #aaa; line-height: 1.4; display: -webkit-box; -webkit-line-clamp: 3; -webkit-box-orient: vertical; overflow: hidden; white-space: pre-wrap;"><%= partner ? partner.description : 'Your club description will appear here on the public network directory.' %></p>

                    <div style="display: flex; gap: 6px; flex-wrap: wrap;">
                        <span class="btn-cc btn-small" style="font-size: 0.65rem; pointer-events: none; opacity: 0.7;">Discord</span>
                        <span class="btn-cc btn-small" style="font-size: 0.65rem; pointer-events: none; opacity: 0.7;">VRChat</span>
                        <span class="btn-cc btn-small" style="font-size: 0.65rem; pointer-events: none; opacity: 0.7;">Partner Page</span>
                    </div>
                </div>
            </div>
        </div>

    </div>

</div>

<!-- ADD/EDIT PARTNER EVENT MODAL -->
<div id="event-modal" style="display: none; position: fixed; inset: 0; background: rgba(0,0,0,0.85); z-index: 9999; align-items: center; justify-content: center; backdrop-filter: blur(8px); overflow-y: auto; padding: 20px;">
    <div class="staff-section" style="width: 540px; max-width: 95%; border: 1px solid var(--primary-pink); box-shadow: 0 15px 40px rgba(0,0,0,0.8); max-height: 90vh; overflow-y: auto;">
        <h3 id="event-modal-title" style="margin-top: 0; color: #fff; border-bottom: 1px solid rgba(255,255,255,0.08); padding-bottom: 10px;">
            📅 Schedule Partner Event
        </h3>
        
        <form onsubmit="handleCreateEvent(event)">
            <input type="hidden" id="evt-id">

            <div class="form-group">
                <label>Event Title *</label>
                <input type="text" id="evt-title" placeholder="e.g. Neon Nights Vol. 3" required>
            </div>

            <!-- EVENT FLYER BANNER ENTRYPOINT -->
            <div class="form-group">
                <label style="color: var(--primary-pink); font-weight: bold;">Event Flyer / Banner Image (Optional)</label>
                <input type="hidden" id="evt-banner-url">
                <div id="evt-flyer-preview-box" style="display: none; width: 100%; height: 110px; border-radius: 8px; overflow: hidden; margin-bottom: 8px; border: 1px solid rgba(255,255,255,0.2); background: #000; position: relative;">
                    <img id="evt-flyer-img" src="" style="width: 100%; height: 100%; object-fit: cover;">
                </div>
                <input type="file" id="evt-flyer-file-input" accept="image/*" style="display: none;" onchange="uploadEventFlyerMedia()">
                <button type="button" class="btn-cc btn-small btn-secondary" onclick="document.getElementById('evt-flyer-file-input').click()" style="width: 100%;">
                    🖼️ Upload Event Flyer / Banner Image
                </button>
            </div>

            <div style="display: grid; grid-template-columns: 1fr 1fr; gap: 10px;">
                <div class="form-group">
                    <label>Start Time *</label>
                    <input type="datetime-local" id="evt-start" required style="color: #fff;">
                </div>
                <div class="form-group">
                    <label>End Time *</label>
                    <input type="datetime-local" id="evt-end" required style="color: #fff;">
                </div>
            </div>

            <div class="form-group">
                <label style="color: var(--primary-blue);">Your Timezone (Auto-converts all times to UTC) *</label>
                <select id="evt-timezone">
                    <option value="UTC">UTC / GMT (Coordinated Universal Time)</option>
                    <option value="America/New_York">US Eastern (EST/EDT)</option>
                    <option value="America/Chicago">US Central (CST/CDT)</option>
                    <option value="America/Denver">US Mountain (MST/MDT)</option>
                    <option value="America/Los_Angeles">US Pacific (PST/PDT)</option>
                    <option value="Europe/London">UK / London (GMT/BST)</option>
                    <option value="Europe/Berlin">Central Europe (CET/CEST)</option>
                    <option value="Asia/Tokyo">Japan (JST)</option>
                    <option value="Australia/Sydney">Australia Eastern (AEST/AEDT)</option>
                </select>
            </div>

            <!-- STRUCTURED DJ LINEUP BUILDER -->
            <div style="background: rgba(0,0,0,0.25); border: 1px solid rgba(255,255,255,0.08); border-radius: 10px; padding: 12px; margin-bottom: 15px;">
                <div style="display: flex; align-items: center; justify-content: space-between; margin-bottom: 10px;">
                    <label style="font-size: 0.85rem; font-weight: bold; color: var(--primary-blue); margin: 0;">
                        🎧 DJ Lineup Timetable
                    </label>
                    <button type="button" class="btn-cc btn-small btn-primary" onclick="addDjLineupSlot()" style="padding: 3px 10px; font-size: 0.75rem;">
                        + Add DJ Slot
                    </button>
                </div>
                <div id="dj-lineup-slots" style="display: flex; flex-direction: column; gap: 8px;"></div>
            </div>

            <div class="form-group">
                <label>Additional Event Details & Description</label>
                <textarea id="evt-desc" rows="3" placeholder="Additional event details, VRChat world info, or guidelines..."></textarea>
            </div>

            <div class="form-group" style="margin-bottom: 15px;">
                <label>Event / World URL (Optional)</label>
                <input type="url" id="evt-url" placeholder="https://vrc.group/... or VRChat world link">
            </div>

            <!-- OFFICIAL LIVE STREAM OPTIONS (STAFF ONLY) -->
            <% if (isStaff) { %>
                <div style="background: rgba(88, 101, 242, 0.08); border: 1px solid rgba(88, 101, 242, 0.3); border-radius: 10px; padding: 14px; margin-bottom: 20px;">
                    <label style="display: flex; align-items: center; gap: 10px; cursor: pointer; color: #fff; font-weight: bold; font-size: 0.9rem; margin-bottom: 6px;">
                        <input type="checkbox" id="evt-is-streamed" style="width: 18px; height: 18px; accent-color: #5865F2;" onchange="toggleStreamLinksBox(this.checked)">
                        <span>📡 Official Live Stream by Club FuRN (Staff Only)</span>
                    </label>
                    <p style="margin: 0 0 8px 0; font-size: 0.78rem; color: #aaa;">Enable this if Club FuRN is officially streaming/broadcasting this event live on Twitch, YouTube, or TikTok.</p>

                    <div id="evt-stream-links-box" style="display: none; border-top: 1px solid rgba(255,255,255,0.08); padding-top: 10px; margin-top: 10px; flex-direction: column; gap: 8px;">
                        <div class="form-group" style="margin-bottom: 6px;">
                            <label style="font-size: 0.78rem; color: #a855f7;">📺 Twitch Stream URL</label>
                            <input type="url" id="evt-stream-twitch" placeholder="https://twitch.tv/clubfurn" style="padding: 6px 10px; font-size: 0.8rem;">
                        </div>
                        <div class="form-group" style="margin-bottom: 6px;">
                            <label style="font-size: 0.78rem; color: #ff0000;">🔴 YouTube Stream URL</label>
                            <input type="url" id="evt-stream-youtube" placeholder="https://youtube.com/@clubfurn/live" style="padding: 6px 10px; font-size: 0.8rem;">
                        </div>
                        <div class="form-group" style="margin-bottom: 6px;">
                            <label style="font-size: 0.78rem; color: #00f2fe;">🎵 TikTok Stream URL</label>
                            <input type="url" id="evt-stream-tiktok" placeholder="https://tiktok.com/@clubfurn/live" style="padding: 6px 10px; font-size: 0.8rem;">
                        </div>
                        <div class="form-group" style="margin-bottom: 0;">
                            <label style="font-size: 0.78rem; color: #53fc18;">⚡ Kick / Custom Stream URL</label>
                            <input type="url" id="evt-stream-other" placeholder="https://kick.com/clubfurn" style="padding: 6px 10px; font-size: 0.8rem;">
                        </div>
                    </div>
                </div>
            <% } else { %>
                <input type="checkbox" id="evt-is-streamed" style="display: none;">
                <input type="hidden" id="evt-stream-twitch" value="">
                <input type="hidden" id="evt-stream-youtube" value="">
                <input type="hidden" id="evt-stream-tiktok" value="">
                <input type="hidden" id="evt-stream-other" value="">
            <% } %>

            <div style="display: flex; gap: 10px; justify-content: flex-end;">
                <button type="button" class="btn-cc btn-secondary" onclick="closeAddEventModal()">Cancel</button>
                <button type="submit" class="btn-cc btn-primary">Save Event</button>
            </div>
        </form>
    </div>
</div>

<script>
    const partnerEventsData = <%- JSON.stringify((partner && partner.events) ? partner.events : []) %>;

    function triggerEditEvent(eventId) {
        const evt = (partnerEventsData || []).find(e => e.id === eventId);
        if (evt) openEditEventModal(evt);
    }

    function updatePreviewColor(color) {
        const card = document.getElementById('preview-card');
        const iconBox = document.getElementById('icon-preview-box');
        if (card) card.style.borderColor = color;
        if (iconBox) iconBox.style.borderColor = color;
    }

    // Live text update handlers for preview
    document.getElementById('partner-name').addEventListener('input', (e) => {
        document.getElementById('card-name-preview').innerText = e.target.value.trim() || 'Your Club Name';
    });

    document.getElementById('partner-description').addEventListener('input', (e) => {
        document.getElementById('card-desc-preview').innerText = e.target.value.trim() || 'Your club description will appear here on the public network directory.';
    });

    let djSlotCounter = 0;
    function addDjLineupSlot(data = {}) {
        const container = document.getElementById('dj-lineup-slots');
        const slotId = `dj-slot-${++djSlotCounter}`;
        const div = document.createElement('div');
        div.id = slotId;
        div.className = 'dj-lineup-row';
        div.style.cssText = 'display: grid; grid-template-columns: 85px 85px 1fr 1fr 28px; gap: 6px; align-items: center; background: rgba(0,0,0,0.3); padding: 8px; border-radius: 6px; border: 1px solid rgba(255,255,255,0.05);';
        
        div.innerHTML = `
            <input type="time" class="dj-start-time input-cc" value="${data.start || ''}" style="padding: 5px; font-size: 0.75rem; color: #fff; width: 100%; box-sizing: border-box;" title="Start Time">
            <input type="time" class="dj-end-time input-cc" value="${data.end || ''}" style="padding: 5px; font-size: 0.75rem; color: #fff; width: 100%; box-sizing: border-box;" title="End Time">
            <input type="text" class="dj-name input-cc" placeholder="DJ / Performer" value="${data.name || ''}" style="padding: 5px 8px; font-size: 0.75rem; width: 100%; box-sizing: border-box;" title="DJ Name">
            <input type="text" class="dj-genre input-cc" placeholder="Genre (e.g. House)" value="${data.genre || ''}" style="padding: 5px 8px; font-size: 0.75rem; width: 100%; box-sizing: border-box;" title="Genre">
            <button type="button" class="btn-cc btn-small" onclick="document.getElementById('${slotId}').remove()" style="padding: 4px; color: #ff4444; border-color: rgba(255,68,68,0.3); font-size: 0.75rem; width: 100%; text-align: center;" title="Remove DJ">&times;</button>
        `;
        container.appendChild(div);
    }

    function formatForDatetimeLocal(isoStr) {
        if (!isoStr) return '';
        const d = new Date(isoStr);
        const pad = n => String(n).padStart(2, '0');
        return `${d.getFullYear()}-${pad(d.getMonth() + 1)}-${pad(d.getDate())}T${pad(d.getHours())}:${pad(d.getMinutes())}`;
    }

    function toggleStreamLinksBox(show) {
        const box = document.getElementById('evt-stream-links-box');
        if (box) box.style.display = show ? 'flex' : 'none';
    }

    function openEditEventModal(evt) {
        document.getElementById('event-modal-title').innerText = '✏️ Edit Partner Event';
        document.getElementById('evt-id').value = evt.id;
        document.getElementById('evt-title').value = evt.title || '';

        const evtTz = evt.timezone || 'UTC';
        const tzSelect = document.getElementById('evt-timezone');
        if (tzSelect && evtTz) tzSelect.value = evtTz;

        document.getElementById('evt-start').value = formatForDatetimeLocal(evt.startTime);
        document.getElementById('evt-end').value = formatForDatetimeLocal(evt.endTime);
        document.getElementById('evt-url').value = evt.eventUrl || '';

        const isStreamed = evt.isStreamedByClubFurn === true;
        document.getElementById('evt-is-streamed').checked = isStreamed;
        toggleStreamLinksBox(isStreamed);

        let streamUrlsObj = {};
        if (evt.streamUrls) {
            try { streamUrlsObj = typeof evt.streamUrls === 'string' ? JSON.parse(evt.streamUrls) : evt.streamUrls; } catch(e) {}
        }
        document.getElementById('evt-stream-twitch').value = streamUrlsObj.twitch || '';
        document.getElementById('evt-stream-youtube').value = streamUrlsObj.youtube || '';
        document.getElementById('evt-stream-tiktok').value = streamUrlsObj.tiktok || '';
        document.getElementById('evt-stream-other').value = streamUrlsObj.other || '';
        
        if (evt.bannerUrl) {
            document.getElementById('evt-banner-url').value = evt.bannerUrl;
            document.getElementById('evt-flyer-img').src = evt.bannerUrl;
            document.getElementById('evt-flyer-preview-box').style.display = 'block';
        } else {
            document.getElementById('evt-banner-url').value = '';
            document.getElementById('evt-flyer-img').src = '';
            document.getElementById('evt-flyer-preview-box').style.display = 'none';
        }

        document.getElementById('dj-lineup-slots').innerHTML = '';
        let desc = evt.description || '';

        // Check for structured lineup JSON first
        let lineupArray = [];
        if (evt.lineup) {
            try { lineupArray = typeof evt.lineup === 'string' ? JSON.parse(evt.lineup) : evt.lineup; } catch(e) {}
        }

        if (Array.isArray(lineupArray) && lineupArray.length > 0) {
            lineupArray.forEach(slot => {
                let startStr = '', endStr = '';
                if (slot.startUtc) {
                    const d = new Date(slot.startUtc);
                    startStr = `${String(d.getHours()).padStart(2, '0')}:${String(d.getMinutes()).padStart(2, '0')}`;
                }
                if (slot.endUtc) {
                    const d = new Date(slot.endUtc);
                    endStr = `${String(d.getHours()).padStart(2, '0')}:${String(d.getMinutes()).padStart(2, '0')}`;
                }
                addDjLineupSlot({ start: startStr || slot.start, end: endStr || slot.end, name: slot.djName || slot.name, genre: slot.genre });
            });
            document.getElementById('evt-desc').value = desc.trim();
        } else {
            document.getElementById('evt-desc').value = desc.trim();
        }

        document.getElementById('event-modal').style.display = 'flex';
    }

    function openAddEventModal() {
        document.getElementById('event-modal-title').innerText = '📅 Schedule Partner Event';
        document.getElementById('evt-id').value = '';
        document.getElementById('evt-title').value = '';
        document.getElementById('evt-start').value = '';
        document.getElementById('evt-end').value = '';
        document.getElementById('evt-url').value = '';
        document.getElementById('evt-desc').value = '';
        document.getElementById('evt-banner-url').value = '';
        document.getElementById('evt-flyer-img').src = '';
        document.getElementById('evt-flyer-preview-box').style.display = 'none';
        document.getElementById('dj-lineup-slots').innerHTML = '';

        document.getElementById('evt-is-streamed').checked = false;
        document.getElementById('evt-stream-twitch').value = '';
        document.getElementById('evt-stream-youtube').value = '';
        document.getElementById('evt-stream-tiktok').value = '';
        document.getElementById('evt-stream-other').value = '';
        toggleStreamLinksBox(false);

        try {
            const userTz = Intl.DateTimeFormat().resolvedOptions().timeZone;
            const tzSelect = document.getElementById('evt-timezone');
            if (tzSelect && userTz) {
                for (let opt of tzSelect.options) {
                    if (opt.value === userTz) { opt.selected = true; break; }
                }
            }
        } catch(e) {}

        document.getElementById('event-modal').style.display = 'flex';
    }

    async function uploadEventFlyerMedia() {
        const fileInput = document.getElementById('evt-flyer-file-input');
        if (!fileInput.files || !fileInput.files[0]) return;

        const file = fileInput.files[0];
        if (file.size > 10 * 1024 * 1024) {
            return showNotify('Flyer image is too large! Maximum allowed size is 10MB.', 'error');
        }

        const formData = new FormData();
        formData.append('flyer', file);

        showNotify('Uploading event flyer...', 'info');

        try {
            const res = await fetch('/api/partner/events/upload-flyer', {
                method: 'POST',
                body: formData
            });

            const data = await res.json();
            if (data.success) {
                document.getElementById('evt-banner-url').value = data.flyerUrl;
                document.getElementById('evt-flyer-img').src = data.flyerUrl;
                document.getElementById('evt-flyer-preview-box').style.display = 'block';
                showNotify('Event flyer uploaded successfully!', 'success');
            } else {
                showNotify(data.error || 'Failed to upload event flyer', 'error');
            }
        } catch (err) {
            console.error(err);
            showNotify('Error uploading event flyer', 'error');
        }
    }

    async function uploadMedia(type) {
        const fileInput = document.getElementById(`${type}-file-input`);
        if (!fileInput.files || !fileInput.files[0]) return;

        const file = fileInput.files[0];
        if (file.size > 10 * 1024 * 1024) {
            return showNotify('File is too large! Maximum allowed size is 10MB.', 'error');
        }

        const formData = new FormData();
        const partnerIdEl = document.getElementById('partner-id');
        if (partnerIdEl && partnerIdEl.value) formData.append('partnerId', partnerIdEl.value);
        formData.append(type, file);

        showNotify(`Uploading ${type}...`, 'info');

        try {
            const endpoint = `/api/partner/upload-${type}`;
            const res = await fetch(endpoint, {
                method: 'POST',
                body: formData
            });

            const data = await res.json();
            if (data.success) {
                const url = type === 'icon' ? data.iconUrl : data.bannerUrl;
                document.getElementById(`${type}-preview`).src = url;
                document.getElementById(`card-${type}-preview`).src = url;
                showNotify(`Club ${type} uploaded successfully!`, 'success');
            } else {
                showNotify(data.error || `Failed to upload ${type}`, 'error');
            }
        } catch (err) {
            console.error(err);
            showNotify(`Error uploading ${type}`, 'error');
        }
    }

    async function handlePartnerSubmit(event) {
        event.preventDefault();

        const partnerIdEl = document.getElementById('partner-id');
        const payload = {
            id: partnerIdEl ? partnerIdEl.value : undefined,
            name: document.getElementById('partner-name').value.trim(),
            customSlug: document.getElementById('partner-slug').value.trim(),
            accentColor: document.getElementById('partner-color').value.trim(),
            description: document.getElementById('partner-description').value.trim(),
            vrcGroupUrl: document.getElementById('partner-vrcgroup').value.trim(),
            discordInvite: document.getElementById('partner-discord').value.trim(),
            websiteUrl: document.getElementById('partner-website').value.trim()
        };

        try {
            const res = await fetch('/api/partner/update', {
                method: 'POST',
                headers: { 'Content-Type': 'application/json' },
                body: JSON.stringify(payload)
            });

            const data = await res.json();
            if (data.success) {
                showNotify('Partner profile saved!', 'success');
                setTimeout(() => window.location.reload(), 1000);
            } else {
                showNotify(data.error || 'Failed to save partner profile', 'error');
            }
        } catch (err) {
            console.error(err);
            showNotify('Network error saving profile', 'error');
        }
    }

    function openAddEventModal() {
        document.getElementById('event-modal-title').innerText = '📅 Schedule Partner Event';
        document.getElementById('evt-id').value = '';
        document.getElementById('evt-title').value = '';
        document.getElementById('evt-start').value = '';
        document.getElementById('evt-end').value = '';
        document.getElementById('evt-url').value = '';
        document.getElementById('evt-desc').value = '';
        document.getElementById('evt-banner-url').value = '';
        document.getElementById('evt-flyer-img').src = '';
        document.getElementById('evt-flyer-preview-box').style.display = 'none';
        document.getElementById('dj-lineup-slots').innerHTML = '';

        try {
            const userTz = Intl.DateTimeFormat().resolvedOptions().timeZone;
            const tzSelect = document.getElementById('evt-timezone');
            if (tzSelect && userTz) {
                for (let opt of tzSelect.options) {
                    if (opt.value === userTz) { opt.selected = true; break; }
                }
            }
        } catch(e) {}

        document.getElementById('event-modal').style.display = 'flex';
    }

    function closeAddEventModal() {
        document.getElementById('event-modal').style.display = 'none';
    }

    function convertLocalTzToUtc(datetimeLocalStr, timezoneStr) {
        if (!datetimeLocalStr) return null;
        try {
            const [datePart, timePart] = datetimeLocalStr.split('T');
            const [year, month, day] = datePart.split('-').map(Number);
            const [hours, minutes] = timePart.split(':').map(Number);

            const dummyDate = new Date(Date.UTC(year, month - 1, day, hours, minutes));
            const formatter = new Intl.DateTimeFormat('en-US', {
                timeZone: timezoneStr,
                year: 'numeric', month: '2-digit', day: '2-digit',
                hour: '2-digit', minute: '2-digit', second: '2-digit',
                hour12: false
            });
            const parts = formatter.formatToParts(dummyDate);
            const p = {};
            parts.forEach(pt => p[pt.type] = pt.value);
            
            const tzYear = parseInt(p.year, 10);
            const tzMonth = parseInt(p.month, 10);
            const tzDay = parseInt(p.day, 10);
            const tzHour = parseInt(p.hour === '24' ? '0' : p.hour, 10);
            const tzMin = parseInt(p.minute, 10);

            const tzAsUtc = Date.UTC(tzYear, tzMonth - 1, tzDay, tzHour, tzMin);
            const dummyAsUtc = dummyDate.getTime();
            const diffMs = tzAsUtc - dummyAsUtc;

            return new Date(dummyAsUtc - diffMs).toISOString();
        } catch (e) {
            return new Date(datetimeLocalStr).toISOString();
        }
    }

    async function handleCreateEvent(event) {
        event.preventDefault();
        const partnerIdEl = document.getElementById('partner-id');
        if (!partnerIdEl) return showNotify('Save partner profile first!', 'error');

        const eventId = document.getElementById('evt-id').value;
        const timezone = document.getElementById('evt-timezone').value;
        const startLocal = document.getElementById('evt-start').value;
        const endLocal = document.getElementById('evt-end').value;

        const startUtc = convertLocalTzToUtc(startLocal, timezone);
        const endUtc = convertLocalTzToUtc(endLocal, timezone);

        if (new Date(endUtc) <= new Date(startUtc)) {
            return showNotify('End Time must be set after Start Time!', 'error');
        }

        // Build DJ Lineup array with UTC timestamps for timezone sync
        const lineupRows = document.querySelectorAll('.dj-lineup-row');
        const lineupData = [];
        const eventStartDate = startLocal ? startLocal.split('T')[0] : '';

        if (lineupRows.length > 0 && eventStartDate) {
            lineupRows.forEach(row => {
                const djStart = row.querySelector('.dj-start-time').value;
                const djEnd = row.querySelector('.dj-end-time').value;
                const djName = row.querySelector('.dj-name').value.trim();
                const genre = row.querySelector('.dj-genre').value.trim();

                if (djName) {
                    let slotStartUtc = null;
                    let slotEndUtc = null;

                    if (djStart) {
                        slotStartUtc = convertLocalTzToUtc(`${eventStartDate}T${djStart}`, timezone);
                    }
                    if (djEnd) {
                        slotEndUtc = convertLocalTzToUtc(`${eventStartDate}T${djEnd}`, timezone);
                    }

                    lineupData.push({
                        djName,
                        genre,
                        start: djStart,
                        end: djEnd,
                        startUtc: slotStartUtc,
                        endUtc: slotEndUtc
                    });
                }
            });
        }

        const customBio = document.getElementById('evt-desc').value.trim();

        const isStreamed = document.getElementById('evt-is-streamed').checked;
        const streamUrlsPayload = {
            twitch: document.getElementById('evt-stream-twitch').value.trim(),
            youtube: document.getElementById('evt-stream-youtube').value.trim(),
            tiktok: document.getElementById('evt-stream-tiktok').value.trim(),
            other: document.getElementById('evt-stream-other').value.trim()
        };

        const payload = {
            partnerId: partnerIdEl.value,
            title: document.getElementById('evt-title').value.trim(),
            startTime: startUtc,
            endTime: endUtc,
            timezone: timezone,
            description: customBio,
            lineup: lineupData,
            eventUrl: document.getElementById('evt-url').value.trim(),
            bannerUrl: document.getElementById('evt-banner-url').value,
            isStreamedByClubFurn: isStreamed,
            streamUrls: streamUrlsPayload
        };

        const endpoint = eventId ? `/api/partner/events/${eventId}` : '/api/partner/events/create';
        const method = eventId ? 'PUT' : 'POST';

        try {
            const res = await fetch(endpoint, {
                method,
                headers: { 'Content-Type': 'application/json' },
                body: JSON.stringify(payload)
            });
            const data = await res.json();
            if (data.success) {
                showNotify(eventId ? 'Partner event updated!' : 'Partner event created!', 'success');
                setTimeout(() => window.location.reload(), 1000);
            } else {
                showNotify(data.error || 'Failed to save event', 'error');
            }
        } catch (err) {
            showNotify('Error saving event', 'error');
        }
    }

    // --- DELETE EVENT MODAL LOGIC ---
    let pendingDeleteEventId = null;

    function openDeleteEventModal(eventId) {
        pendingDeleteEventId = eventId;
        const modal = document.getElementById('delete-event-modal');
        if (modal) modal.style.display = 'flex';
    }

    function closeDeleteEventModal() {
        pendingDeleteEventId = null;
        const modal = document.getElementById('delete-event-modal');
        if (modal) modal.style.display = 'none';
    }

    async function confirmDeletePartnerEvent() {
        if (!pendingDeleteEventId) return;
        const btn = document.getElementById('delete-event-confirm-btn');
        if (btn) {
            btn.disabled = true;
            btn.innerText = 'Deleting...';
        }

        try {
            const res = await fetch(`/api/partner/events/${pendingDeleteEventId}`, {
                method: 'DELETE'
            });
            const data = await res.json();
            closeDeleteEventModal();
            if (data.success) {
                showNotify('Event deleted successfully', 'success');
                setTimeout(() => window.location.reload(), 500);
            } else {
                showNotify(data.error || 'Failed to delete event', 'error');
            }
        } catch (err) {
            closeDeleteEventModal();
            showNotify('Error deleting event', 'error');
        } finally {
            if (btn) {
                btn.disabled = false;
                btn.innerText = 'Delete Event';
            }
        }
    }

    let activeDiscordEventId = null;

    function openDiscordPostModal(eventId, eventTitle) {
        activeDiscordEventId = eventId;
        const modal = document.getElementById('discord-post-modal');
        const titleEl = document.getElementById('discord-modal-event-title');
        if (titleEl) titleEl.innerText = `Event: "${eventTitle}"`;
        if (modal) modal.style.display = 'flex';
    }

    function closeDiscordPostModal() {
        activeDiscordEventId = null;
        const modal = document.getElementById('discord-post-modal');
        if (modal) modal.style.display = 'none';
    }

    async function confirmPostDiscordAnnouncement() {
        if (!activeDiscordEventId) return;
        const confirmBtn = document.getElementById('discord-modal-confirm-btn');
        if (confirmBtn) {
            confirmBtn.disabled = true;
            confirmBtn.innerText = 'Posting...';
        }

        try {
            const res = await fetch(`/api/partner/events/${activeDiscordEventId}/announce`, {
                method: 'POST',
                headers: { 'Content-Type': 'application/json' }
            });
            const data = await res.json();
            closeDiscordPostModal();

            if (res.ok && data.success) {
                showNotify('Announcement posted to Discord successfully!', 'success');
                setTimeout(() => window.location.reload(), 1000);
            } else {
                showNotify(data.error || 'Failed to post to Discord', 'error');
            }
        } catch(e) {
            closeDiscordPostModal();
            showNotify('Network error posting announcement', 'error');
        } finally {
            if (confirmBtn) {
                confirmBtn.disabled = false;
                confirmBtn.innerText = 'Post Announcement Now';
            }
        }
    }

    // --- CO-OWNERS MANAGEMENT HANDLERS ---
    async function addCoOwner() {
        const partnerId = document.getElementById('partner-id') ? document.getElementById('partner-id').value : null;
        if (!partnerId) {
            showNotify('Please save your partner club first before adding co-owners.', 'error');
            return;
        }

        const input = document.getElementById('add-co-owner-input');
        const discordId = input ? input.value.trim() : '';
        if (!discordId) {
            showNotify('Please enter a Discord ID to add as a co-owner.', 'error');
            return;
        }

        try {
            const res = await fetch(`/api/partner/${partnerId}/co-owners/add`, {
                method: 'POST',
                headers: { 'Content-Type': 'application/json' },
                body: JSON.stringify({ targetDiscordId: discordId })
            });
            const data = await res.json();
            if (res.ok && data.success) {
                showNotify(`Successfully added ${data.addedUser ? data.addedUser.name : 'user'} as a Co-Owner!`, 'success');
                setTimeout(() => window.location.reload(), 1000);
            } else {
                showNotify(data.error || 'Failed to add co-owner', 'error');
            }
        } catch(e) {
            showNotify('Network error adding co-owner', 'error');
        }
    }

    // --- REMOVE CO-OWNER MODAL LOGIC ---
    let pendingRemoveCoOwnerId = null;

    function openRemoveCoOwnerModal(discordId, name) {
        pendingRemoveCoOwnerId = discordId;
        const nameEl = document.getElementById('remove-coowner-name');
        if (nameEl) nameEl.innerText = name;
        const modal = document.getElementById('remove-coowner-modal');
        if (modal) modal.style.display = 'flex';
    }

    function closeRemoveCoOwnerModal() {
        pendingRemoveCoOwnerId = null;
        const modal = document.getElementById('remove-coowner-modal');
        if (modal) modal.style.display = 'none';
    }

    async function confirmRemoveCoOwner() {
        const partnerId = document.getElementById('partner-id') ? document.getElementById('partner-id').value : null;
        if (!partnerId || !pendingRemoveCoOwnerId) return;

        const btn = document.getElementById('remove-coowner-confirm-btn');
        if (btn) {
            btn.disabled = true;
            btn.innerText = 'Removing...';
        }

        try {
            const res = await fetch(`/api/partner/${partnerId}/co-owners/${pendingRemoveCoOwnerId}`, {
                method: 'DELETE'
            });
            const data = await res.json();
            closeRemoveCoOwnerModal();
            if (res.ok && data.success) {
                showNotify('Removed co-owner successfully.', 'success');
                setTimeout(() => window.location.reload(), 1000);
            } else {
                showNotify(data.error || 'Failed to remove co-owner', 'error');
            }
        } catch(e) {
            closeRemoveCoOwnerModal();
            showNotify('Network error removing co-owner', 'error');
        } finally {
            if (btn) {
                btn.disabled = false;
                btn.innerText = '✕ Remove Co-Owner';
            }
        }
    }

    // --- TRANSFER OWNERSHIP MODAL LOGIC ---
    let pendingTransferDiscordId = null;

    function openTransferModal(discordId, name) {
        pendingTransferDiscordId = discordId;
        const nameEl = document.getElementById('transfer-owner-target-name');
        if (nameEl) nameEl.innerText = name;
        const modal = document.getElementById('transfer-owner-modal');
        if (modal) modal.style.display = 'flex';
    }

    function closeTransferModal() {
        pendingTransferDiscordId = null;
        const modal = document.getElementById('transfer-owner-modal');
        if (modal) modal.style.display = 'none';
    }

    async function confirmTransferOwnership() {
        const partnerId = document.getElementById('partner-id') ? document.getElementById('partner-id').value : null;
        if (!partnerId || !pendingTransferDiscordId) return;

        const btn = document.getElementById('transfer-owner-confirm-btn');
        if (btn) {
            btn.disabled = true;
            btn.innerText = 'Transferring...';
        }

        try {
            const res = await fetch(`/api/partner/${partnerId}/transfer-ownership`, {
                method: 'POST',
                headers: { 'Content-Type': 'application/json' },
                body: JSON.stringify({ targetDiscordId: pendingTransferDiscordId })
            });
            const data = await res.json();
            closeTransferModal();
            if (res.ok && data.success) {
                showNotify('Primary ownership transferred successfully!', 'success');
                setTimeout(() => window.location.reload(), 1200);
            } else {
                showNotify(data.error || 'Failed to transfer ownership', 'error');
            }
        } catch(e) {
            closeTransferModal();
            showNotify('Network error transferring ownership', 'error');
        } finally {
            if (btn) {
                btn.disabled = false;
                btn.innerText = '👑 Confirm & Transfer Ownership';
            }
        }
    }
</script>

<!-- DISCORD POST CONFIRMATION MODAL -->
<div id="discord-post-modal" style="display: none; position: fixed; inset: 0; background: rgba(0,0,0,0.85); z-index: 10000; align-items: center; justify-content: center; backdrop-filter: blur(8px);">
    <div style="background: #121821; border: 1px solid #5865F2; border-radius: 16px; width: 90%; max-width: 480px; padding: 25px; box-shadow: 0 15px 40px rgba(88,101,242,0.3); position: relative; text-align: left;">
        <h3 style="margin-top: 0; margin-bottom: 8px; color: #fff; font-size: 1.2rem; display: flex; align-items: center; gap: 8px;">
            <span style="color: #5865F2;">📢</span> Post Announcement to Discord
        </h3>
        
        <p id="discord-modal-event-title" style="font-size: 0.9rem; color: var(--primary-blue); font-weight: bold; margin-bottom: 15px;"></p>
        
        <div style="font-size: 0.85rem; color: #cbd5e1; line-height: 1.6; margin-bottom: 20px; background: rgba(88,101,242,0.1); border: 1px solid rgba(88,101,242,0.25); padding: 14px; border-radius: 10px;">
            You can post an initial event announcement <strong>once</strong> to advertise this upcoming event in Discord.<br><br>
            <span style="color: #5865F2; font-weight: bold;">Note:</span> Once your event goes live, our system will automatically post a live announcement to Discord.
        </div>

        <div style="display: flex; gap: 10px; justify-content: flex-end;">
            <button type="button" class="btn-cc btn-small btn-dark" onclick="closeDiscordPostModal()">
                Cancel
            </button>
            <button id="discord-modal-confirm-btn" type="button" class="btn-cc btn-small btn-primary" style="background: #5865F2; border-color: #5865F2;" onclick="confirmPostDiscordAnnouncement()">
                Post Announcement Now
            </button>
        </div>
    </div>
</div>

<!-- TRANSFER OWNERSHIP CONFIRMATION MODAL -->
<div id="transfer-owner-modal" style="display: none; position: fixed; inset: 0; background: rgba(0,0,0,0.85); z-index: 10000; align-items: center; justify-content: center; backdrop-filter: blur(8px);">
    <div style="background: #121821; border: 1px solid #ffd700; border-radius: 16px; width: 90%; max-width: 480px; padding: 25px; box-shadow: 0 15px 40px rgba(255,215,0,0.25); position: relative; text-align: left;">
        <h3 style="margin-top: 0; margin-bottom: 8px; color: #fff; font-size: 1.2rem; display: flex; align-items: center; gap: 8px;">
            <span style="color: #ffd700;">👑</span> Transfer Club Ownership
        </h3>
        
        <p style="font-size: 0.95rem; color: #fff; line-height: 1.5; margin-bottom: 12px;">
            Are you sure you want to transfer <strong>Primary Ownership</strong> of this club to <strong id="transfer-owner-target-name" style="color: #ffd700;"></strong>?
        </p>
        
        <div style="font-size: 0.85rem; color: #cbd5e1; line-height: 1.5; margin-bottom: 20px; background: rgba(255,215,0,0.08); border: 1px solid rgba(255,215,0,0.25); padding: 12px 14px; border-radius: 10px;">
            ⚠️ <strong style="color: #ffd700;">Important:</strong> They will become the primary owner. You will remain an active <strong>Co-Owner</strong> with full editing permissions unless removed.
        </div>

        <div style="display: flex; gap: 10px; justify-content: flex-end;">
            <button type="button" class="btn-cc btn-small btn-dark" onclick="closeTransferModal()">
                Cancel
            </button>
            <button id="transfer-owner-confirm-btn" type="button" class="btn-cc btn-small" style="background: #ffd700; color: #000; font-weight: bold; border-color: #ffd700;" onclick="confirmTransferOwnership()">
                👑 Confirm & Transfer Ownership
            </button>
        </div>
    </div>
</div>

<!-- REMOVE CO-OWNER CONFIRMATION MODAL -->
<div id="remove-coowner-modal" style="display: none; position: fixed; inset: 0; background: rgba(0,0,0,0.85); z-index: 10000; align-items: center; justify-content: center; backdrop-filter: blur(8px);">
    <div style="background: #121821; border: 1px solid #ef4444; border-radius: 16px; width: 90%; max-width: 460px; padding: 25px; box-shadow: 0 15px 40px rgba(239,68,68,0.25); position: relative; text-align: left;">
        <h3 style="margin-top: 0; margin-bottom: 8px; color: #fff; font-size: 1.2rem; display: flex; align-items: center; gap: 8px;">
            <span style="color: #ef4444;">✕</span> Remove Co-Owner
        </h3>
        
        <p style="font-size: 0.95rem; color: #fff; line-height: 1.5; margin-bottom: 12px;">
            Are you sure you want to remove <strong id="remove-coowner-name" style="color: #ef4444;"></strong> from this club's co-owners?
        </p>
        
        <div style="font-size: 0.85rem; color: #cbd5e1; line-height: 1.5; margin-bottom: 20px; background: rgba(239,68,68,0.08); border: 1px solid rgba(239,68,68,0.25); padding: 12px 14px; border-radius: 10px;">
            They will immediately lose access to edit this club profile and manage its showcase events.
        </div>

        <div style="display: flex; gap: 10px; justify-content: flex-end;">
            <button type="button" class="btn-cc btn-small btn-dark" onclick="closeRemoveCoOwnerModal()">
                Cancel
            </button>
            <button id="remove-coowner-confirm-btn" type="button" class="btn-cc btn-small btn-danger" onclick="confirmRemoveCoOwner()">
                ✕ Remove Co-Owner
            </button>
        </div>
    </div>
</div>

<!-- DELETE EVENT CONFIRMATION MODAL -->
<div id="delete-event-modal" style="display: none; position: fixed; inset: 0; background: rgba(0,0,0,0.85); z-index: 10000; align-items: center; justify-content: center; backdrop-filter: blur(8px);">
    <div style="background: #121821; border: 1px solid #ef4444; border-radius: 16px; width: 90%; max-width: 440px; padding: 25px; box-shadow: 0 15px 40px rgba(239,68,68,0.25); position: relative; text-align: left;">
        <h3 style="margin-top: 0; margin-bottom: 8px; color: #fff; font-size: 1.2rem; display: flex; align-items: center; gap: 8px;">
            <span style="color: #ef4444;">🗑️</span> Delete Partner Event
        </h3>
        
        <p style="font-size: 0.95rem; color: #fff; line-height: 1.5; margin-bottom: 20px;">
            Are you sure you want to permanently delete this event? This action cannot be undone.
        </p>

        <div style="display: flex; gap: 10px; justify-content: flex-end;">
            <button type="button" class="btn-cc btn-small btn-dark" onclick="closeDeleteEventModal()">
                Cancel
            </button>
            <button id="delete-event-confirm-btn" type="button" class="btn-cc btn-small btn-danger" onclick="confirmDeletePartnerEvent()">
                Delete Event
            </button>
        </div>
    </div>
</div>

<%- include('footer') %>
